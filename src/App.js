import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom';
import './App.css';

function App() {
  return (
    <BrowserRouter>
      <div class="container">
        <Routes>
        </Routes>
      </div>
    </BrowserRouter>
  );
}

export default App;
